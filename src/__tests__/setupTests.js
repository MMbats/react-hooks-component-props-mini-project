// src/setupTests.js
import '@testing-library/jest-dom'; // Ensure this import is present

// Dummy test to ensure setupTests is being loaded
test('setupTests.js is running', () => {
  expect(true).toBe(true); // Simple assertion to avoid errors
});
